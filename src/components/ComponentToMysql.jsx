import React from "react";
const axios = require('axios');

export const ComponentToMysql = React.forwardRef((props) => {
    
    const {cart, totalAmount} = props;

    const transact = () => {

        }

    return (
        
    )

});